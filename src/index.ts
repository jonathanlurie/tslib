// Importing a type...
import type { someClassOptionsType } from "./SomeClass";

// Importing a class
import SomeClass from "./SomeClass";

// Exporting all the types that need to be exposed to the users of the lib
export type { someClassOptionsType };

// Exporting classes, objects, functions, etc.
export { SomeClass };
